# Copy to .env.local (git-ignored)
TOPDECK_API_KEY=
# Optional: allow the polite MTGGoldfish scraper (1 req/s, 24h cache)
META_GOLDFISH_ENABLED=0
# Optional: absolute path to the repo root if auto-detection fails
# MTG_ROOT=
