export const meta = {
  name: 'phase-8a1-core-registry',
  description: 'Phase 8a-1: engine registries, hook points, declaration merging and ext bags — one Opus implementer, three adversarial reviewers, one fix round',
  phases: [{ title: 'Implement' }, { title: 'Review' }, { title: 'Fix' }],
}

const ROOT = 'C:/Users/vprog/dev/mark-magic'
const PLAN = 'C:/Users/vprog/.claude/plans/immutable-greeting-puppy.md'

const IMPL = { type: 'object', properties: {
  summary: { type: 'string' },
  filesChanged: { type: 'array', items: { type: 'string' } },
  filesAdded: { type: 'array', items: { type: 'string' } },
  hooks: { type: 'array', items: { type: 'object', properties: { name: { type: 'string' }, file: { type: 'string' }, fn: { type: 'string' } }, required: ['name', 'file', 'fn'] } },
  proofs: { type: 'object', properties: { typecheckAll: { type: 'string' }, npmTest: { type: 'string' }, coveragePool: { type: 'string' }, verifyPool: { type: 'string' }, smokeVerify: { type: 'string' }, bench: { type: 'string' }, genRegistryIdempotent: { type: 'boolean' } }, required: ['typecheckAll', 'npmTest', 'coveragePool', 'verifyPool', 'smokeVerify', 'bench', 'genRegistryIdempotent'] },
  behaviourIdentical: { type: 'boolean' },
  openIssues: { type: 'array', items: { type: 'string' } },
  notes: { type: 'string' },
}, required: ['summary', 'filesChanged', 'filesAdded', 'hooks', 'proofs', 'behaviourIdentical', 'openIssues'] }

const REVIEW = { type: 'object', properties: {
  refuted: { type: 'boolean' },
  issues: { type: 'array', items: { type: 'object', properties: { severity: { type: 'string', enum: ['blocker', 'major', 'minor'] }, file: { type: 'string' }, claim: { type: 'string' }, evidence: { type: 'string' }, cr: { type: 'string' } }, required: ['severity', 'claim', 'evidence'] } },
  checksRun: { type: 'array', items: { type: 'string' } },
  leftoverFiles: { type: 'array', items: { type: 'string' } },
}, required: ['refuted', 'issues', 'checksRun', 'leftoverFiles'] }

const HARD = `HARD RULES: work in the main checkout ${ROOT} on branch main; nothing else is running there. Do NOT git commit, do not create branches, do not stash. Never create symlinks or junctions. Never run 'npm run web:index'. Never kill node processes you did not start (the user's dev servers are running). Write every file with LF line endings (no \\r). Do not modify anything under data/ except reading it (data/bench/*.json baselines are read-only). Lines in game.ts/parse.ts/types.ts are very long: read with 'sed -n A,Bp file | cut -c1-600' or the Read tool, and edit with exact-string edits — never rewrite whole core files. Keep the code style of the surrounding file (dense, commented one-liners, ./x.js imports, TypeScript 7 strict, NodeNext).`

const IMPL_PROMPT = `You are implementing Phase 8a-1 of the approved plan at ${PLAN}: the engine extensibility contract that lets many agents add mechanic families in parallel WITHOUT editing core files. Read the plan's "Part 1" (sections 1.1 and 1.2 — the hook table and the declaration-merging pattern), the lint bullets at the end of section 2.5, and "Part 8 — Risks" before touching anything. Then read the code you will change: src/cards/types.ts, src/engine/state.ts, src/engine/events.ts, src/engine/clone.ts, src/engine/serialize.ts, src/engine/view.ts, src/engine/characteristics.ts, src/engine/legal.ts, src/engine/cost.ts, src/engine/agents/defaults.ts, and the relevant functions of src/engine/game.ts (applyEffect, queueTriggers, putTriggersOnStack, refreshStaticGrants, castSpell, payCost, enterBattlefield, activateAbility, performAction, moveTo, dealDamage, dealDamageToPlayer, draw, replaceCounters, gainLife, runTurn, runTurnFrom, setStep, combatFrom, combatDamage, checkSBA, flushDelayed), plus test/lint-direct-writes.test.ts and test/lint-nplayer.test.ts.

BEFORE EDITING: wait until ${ROOT}/data/bench/smoke-commander.json exists (a background job is recording behaviour baselines; poll every 10 s, at most 3 minutes). Record the current numbers: 'npm test' (268 pass), 'npm run coverage:pool' (fully_parsed 11541), 'npm run verify:pool' (sandbox-ok 11064, unreachable 477), data/bench/latest.json (45.8 games/s 60-card, 4.78 Commander).

DELIVERABLES (all of them):
1. src/engine/ops/types.ts — the FamilyModule/OpCtx/Mods/EnterCtx/ZoneMoveOverride/TriggerCtx types exactly as the plan's section 1.1 sketches (effects, conditions, amounts, triggers, statics, costParts, asEnters, replacements{zoneMove,damage,draw,counters,lifeGain}, steps (every Step plus 'turn-start' and 'cleanup-end'), sba, legalActions, actions, decisions, keywordHooks{canBlock,canAttack,blockCheck,blockFixup,combatDamage}, triggerSources, events, redact, cleanupEot, render). Also a small 'ext' helper module (src/engine/ops/ext.ts): typed get/set on GameObject/Player/GameState ext bags, JSON-plain only.
2. src/engine/ops/_registry.ts GENERATED by a new script scripts/gen-registry.mjs (npm script 'gen:registry'): it lists every src/engine/ops/<name>.ts file that does not start with '_' and is not '.schema.ts'/'types.ts'/'ext.ts', imports their default exports, and builds flat lookups: EFFECT_OPS, CONDITIONS, AMOUNTS, TRIGGERS, STATICS, COST_PARTS, AS_ENTERS, REPLACEMENTS, STEP_HOOKS, SBA_HOOKS, LEGAL_PROVIDERS, ACTIONS, DECISION_DEFAULTS, CAN_ATTACK, CAN_BLOCK, BLOCK_CHECKS, BLOCK_FIXUPS, COMBAT_DAMAGE_HOOKS, TRIGGER_SOURCES, EVENT_META, REDACT_HOOKS, EOT_CLEANUP, RENDERERS, plus MODULES and registryHash() (fnv-1a of the sorted op/condition/trigger/static/amount names). Duplicate keys across modules are a hard error naming both files. The generated file must be byte-identical when re-run (deterministic order), and it must also export registerFamily(module) / unregisterFamily(name) so tests can register probe families at runtime (the flat lookups are rebuilt on register). The generator writes LF. Mark the file with a '// GENERATED by scripts/gen-registry.mjs — do not edit' header. The engine imports the barrel (game.ts, characteristics.ts, legal.ts, cost.ts, defaults.ts, events.ts) — make sure there is no import cycle problem at module-evaluation time (families import types from the core; the core imports the barrel; family modules must only use core values lazily inside functions — document this in src/engine/ops/_example.ts).
3. Hook insertions — every row of the plan's 1.1 table, as one-line 'default:' fallbacks or fold loops, in exactly the places named: applyEffect default → EFFECT_OPS (build OpCtx from the locals already computed: g, s, item, p, src, T, idx, amt, objs() via this.objs(T), players(), apply()); conditionHolds default → CONDITIONS; evalAmount default → AMOUNTS; queueTriggers inner switch default → TRIGGERS, and iterate TRIGGER_SOURCES(s) after allPermanents; computeStaticMods → STATICS after the existing kinds, Mods.flags gets a string index signature and a setPT slot applied before additive terms; canAttack/canBlock consult CAN_ATTACK/CAN_BLOCK before 'return true'; combatFrom validates blocks with BLOCK_CHECKS and applies BLOCK_FIXUPS to the declared blocks; nonManaCostPayable + payCost fold COST_PARTS for non-core cost keys (define CORE_COST_KEYS); spellManaCost gains a 'modes' parameter threaded from castSpell/legal so mode costs can be added later (no behaviour change now); legalActions runs LEGAL_PROVIDERS before 'return out' and exports castActionsFor and describeSpec; castSpell's 'from' gate consults CAST_FROM_HOOKS and its free computation consults FREE_CAST_HOOKS (add both to the registry); AltCost.from widens to CastZone; performAction default → ACTIONS; enterBattlefield as-enters default → AS_ENTERS; moveTo (right after the commander redirect) folds REPLACEMENTS.zoneMove (override zone/pos/cancel/emit) and calls LEAVE_HOOKS where 'exile until leaves' is handled; dealDamage/dealDamageToPlayer fold REPLACEMENTS.damage after protection/shield checks; draw folds REPLACEMENTS.draw after the dredge block; replaceCounters folds REPLACEMENTS.counters; gainLife folds REPLACEMENTS.lifeGain; runTurnFrom/runTurn/combatFrom call STEP_HOOKS[step] after each step's built-in work plus 'turn-start' and 'cleanup-end'; the end-of-turn wipe loop calls EOT_CLEANUP; an extra-combat loop driven by s.ext.extraCombats after main 2 (re-run combatFrom then main 2 again); checkSBA runs SBA_HOOKS inside its loop; combatDamage runs COMBAT_DAMAGE_HOOKS after assignments are built and before they are dealt; defaultAnswer default → DECISION_DEFAULTS; events.ts LOGGED/citation/renderEvent consult EVENT_META in their defaults; view.ts redact calls REDACT_HOOKS per object for the viewer; defOf consults o.ext.copyOf resolved through s.ext.defs (a def table keyed by string) so copies survive clone/serialize; targetOptionsFor consults TARGET_KINDS for non-core TargetSpec kinds; the treasure/clue/food/spawn hardcoding in activateAbility/legalActions becomes a TOKEN_ABILITIES registry keyed by token name with the same four entries; allPermanents callers treat o.ext.phasedOut as 'not on the battlefield' via an isPhasedOut(o) helper. Every fold loop must be guarded so the hot path pays one length check when nothing is registered.
4. Declaration merging — plan 1.2 verbatim: rename the unions in src/cards/types.ts to Core* and expose EffectRegistry, ConditionRegistry, TriggerRegistry, StaticRegistry, AmountCountRegistry, KeywordRegistry, AltCostIdRegistry, AsEntersRegistry, AbilityCostExt (AbilityCost extends it), TargetKindRegistry; in state.ts DecisionRegistry, ActionRegistry, DelayedAtRegistry (and re-export AltCostId from types); in events.ts EventRegistry. Public type names (Effect, Condition, TriggerEvent, StaticEffect, Keyword, AltCost, AsEnters, AbilityCost, Decision, PlayerAction, GameEventBody) keep their names so nothing else changes. apps/web must still typecheck ('npm run web:typecheck').
5. ext bags — GameObject.ext?, Player.ext?, GameState.ext? (Record<string, unknown>, JSON-plain); clone.ts deep-copies them with a plain-copy helper (arrays/objects/primitives only); serialize.ts round-trips them; makeObject leaves ext undefined.
6. src/engine/ops/_example.ts — a fully commented template family (excluded from the barrel by its underscore) showing every hook kind, the 'declare module' augmentation for each registry, the ext convention, the no-node-imports rule, and the lazy-import rule. docs/vocabulary/README.md — the family author's guide (how to add a family, run gen:registry, the lint rules, how scenarios are written, how parser rules will hook in — say 'parser rule registry lands in 8a-2').
7. Lints: extend test/lint-direct-writes.test.ts to scan src/engine/ops/**/*.ts with ceiling 0 for every pattern; extend test/lint-nplayer.test.ts to include src/engine/ops; add test/lint-ops.test.ts forbidding 'node:' imports and 'new Set(' / 'new Map(' assignments into ext under src/engine/ops, and asserting the generated barrel is up to date (run the generator into a temp string and compare).
8. test/registry.test.ts — the PROOF: register a probe family at runtime (unregister in 'after') that uses EVERY hook kind at least once (an effect op, a condition, an amount, a trigger matcher raised via g.queueTriggers, a static that sets a flag and setPT, a cost part, an as-enters kind, replacements zoneMove and damage and draw and counters and lifeGain, a step hook, an sba hook, a legal-action provider plus its action, a decision default, canBlock/canAttack, blockCheck/blockFixup, combatDamage, triggerSources, an event with EVENT_META, redact, cleanupEot, a TARGET_KINDS entry, TOKEN_ABILITIES lookup, phasedOut, copyOf via defOf, extra combat) and asserts each one is observed in a real Game built with test/helpers.ts setup(). Use existing real cards (Grizzly Bears, Lightning Bolt, Hill Giant, basic lands).
9. package.json: 'gen:registry' script; 'verify:quick' script = typecheck + node --test on test/lint-*.test.ts test/registry.test.ts test/scripts.test.ts + 'npm run scripts:check'. Keep verify:all as is but add gen:registry idempotence there is NOT needed (the lint covers it).

PROOF OF NO BEHAVIOUR CHANGE (all must hold, report the exact output lines): 'npm run typecheck:all'; 'npm test' all green (268 old + your new tests); 'npm run coverage:pool' still fully_parsed 11541; 'npm run verify:pool' still sandbox-ok 11064 / unreachable 477; 'npm run sim:batch -- --verify data/bench/smoke.json', '... --verify data/bench/smoke2.json' and '... --verify data/bench/smoke-commander.json' each report that the win counts reproduce; 'npm run bench:games' at or above 41 games/s (60-card) and 4.3 (Commander) — if lower, find the hot-path cost you added and remove it (OpCtx must not be allocated per effect when no registry op is involved: build it lazily in the default branch only); 'npm run gen:registry' twice produces no diff; 'git status' shows only intended files, no CRLF ('git ls-files --eol' shows i/lf for changed files; grep -c $'\\r' on each new file is 0). ${HARD}

Return the structured result. In 'hooks' list every hook with its file and function. In 'openIssues' list anything from the plan's 1.1 table you could not do and why — do not silently skip rows.`

const LENSES = [
  { key: 'behaviour', prompt: `Lens: BEHAVIOUR IDENTITY AND ENGINE CORRECTNESS. Independently re-run: 'npm run typecheck:all', 'npm test', 'npm run coverage:pool' (expect fully_parsed 11541), 'npm run verify:pool' (expect sandbox-ok 11064, unreachable 477), 'npm run sim:batch -- --verify data/bench/smoke.json', '--verify data/bench/smoke2.json', '--verify data/bench/smoke-commander.json'. Then read every hook insertion ('git diff' of src/engine and src/cards) and try to refute correctness: a default branch that swallows an existing case; a replacement fold placed before protection/shield/prevention or after the damage was already applied; step hooks placed before triggers are put on the stack or before SBA; the extra-combat loop re-running main 2 incorrectly or leaking s.ext.extraCombats across turns; queueTriggers iterating TRIGGER_SOURCES for events they cannot receive; clone/serialize/redact losing or aliasing ext state (write a throwaway test in your head or a scratch file under C:/Users/vprog/AppData/Local/Temp/claude/ — never inside the repo); N-player assumptions; defOf indirection allocating per call.` },
  { key: 'contract', prompt: `Lens: EXTENSIBILITY CONTRACT COMPLETENESS. Read the plan's Part 1 (1.1, 1.2) and Part 3's family list. Then, WITHOUT editing any existing file, write a real small family as a scratch module src/engine/ops/_review_probe.ts (underscore-prefixed so the barrel ignores it; register it at runtime from a scratch test file test/_review_probe.test.ts) implementing 'dethrone' (CR 702.104: whenever this creature attacks the player with the most life or tied for most life, put a +1/+1 counter on it) AND 'afflict N' (CR 702.131: whenever this creature becomes blocked, defending player loses N life) AND a suspend-style upkeep step hook that removes a time counter from exiled cards and a replacement that redirects a would-die zone move to exile — using ONLY the registry, the 'declare module' augmentations and the ext bags. Run your scratch test. Every place where you needed something the contract does not offer (a missing hook, a missing ctx field, a type you could not augment, an import cycle, a redaction gap) is an issue with severity major or blocker. DELETE both scratch files before returning and confirm with 'git status' that the tree has only the implementer's changes (list anything left over in leftoverFiles).` },
  { key: 'performance', prompt: `Lens: PERFORMANCE AND HYGIENE. Run 'npm run bench:games' twice and compare with data/bench/latest.json from before the change (45.8 games/s 60-card, 4.78 Commander); anything below 41 / 4.3 is a blocker, 41-44 / 4.3-4.6 a major with the suspected hot-path cause. Read the diff for per-call allocations in applyEffect/conditionHolds/evalAmount/queueTriggers/computeStaticMods/moveTo/dealDamage (OpCtx objects, closures, array spreads, Object.keys on costs), unguarded fold loops, and hooks called inside per-permanent loops. Check: 'npm run gen:registry' twice → no diff; the lint tests (test/lint-*.test.ts) actually fail when you temporarily introduce a violation in a scratch file (then delete it); 'npm run web:typecheck' passes; no file has CRLF ('git ls-files --eol' on changed files, grep for \\r in new files); no node: imports under src/engine/ops; the generated barrel and _example.ts are documented; nothing under data/ changed ('git status'). List leftover files.` },
]
const reviewPrompt = (lens, impl, again) => `You are an adversarial reviewer of Phase 8a-1 (engine registries/hooks/declaration merging/ext bags) in ${ROOT} (main checkout, branch main, uncommitted working tree — do NOT commit, do NOT modify the implementer's files, do NOT stash or checkout). The plan is ${PLAN}. The implementer reported: ${JSON.stringify(impl).slice(0, 6000)}. ${again ? 'This is a RE-REVIEW after a fix round: focus on whether the blocker/major issues were actually fixed and nothing regressed.' : ''} Default to refuted=true unless you fail to find a real defect after actually running the checks. ${lens.prompt} Report each issue with severity, file, claim and concrete evidence (command output or line). Never kill node processes you did not start; never create junctions; never run 'npm run web:index'.`

phase('Implement')
const impl = await agent(IMPL_PROMPT, { label: 'impl:8a-1', phase: 'Implement', model: 'opus', effort: 'high', schema: IMPL })
if (!impl) return { failed: 'implementer produced nothing' }
log(`implementer: ${impl.summary.slice(0, 200)} — behaviourIdentical=${impl.behaviourIdentical}, open=${impl.openIssues.length}`)

phase('Review')
const reviews = (await parallel(LENSES.map(l => () => agent(reviewPrompt(l, impl, false), { label: `review:${l.key}`, phase: 'Review', model: 'opus', effort: 'high', schema: REVIEW })))).filter(Boolean)
const blocking = reviews.flatMap(r => r.issues.filter(i => i.severity !== 'minor'))
log(`reviews: ${reviews.length}/3 returned, ${blocking.length} blocker/major issues`)

let fixed = null, rereview = null
if (blocking.length) {
  phase('Fix')
  fixed = await agent(`Fix the following blocker/major review issues from Phase 8a-1 in ${ROOT} (main checkout, uncommitted working tree; do NOT commit). Issues: ${JSON.stringify(blocking)}. Re-run the full PROOF list afterwards (typecheck:all, npm test, coverage:pool 11541, verify:pool 11064/477, the three sim:batch --verify baselines, bench ≥ 41/4.3, gen:registry idempotent, no CRLF) and report it. ${HARD}`, { label: 'fix:8a-1', phase: 'Fix', model: 'opus', effort: 'high', schema: IMPL })
  rereview = await agent(reviewPrompt(LENSES[0], fixed ?? impl, true), { label: 're-review:8a-1', phase: 'Fix', model: 'opus', effort: 'high', schema: REVIEW })
}
return { impl, reviews, blocking, fixed, rereview }