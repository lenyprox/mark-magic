# Brief: parse:why tooling — failure-cause histogram, parser-wave briefs, parser-wave workflows

Plan sections to read first (in `C:/Users/vprog/.claude/plans/silly-honking-quill.md`): "Context", "Parser failure surface", "Workflows and process", and **D1, D2, D3** — this brief is D1–D3 with operational detail; every file:line reference you need is there. Also read src/cards/parse.ts in full around the named lines (the two-pass ladder 1091–1130, parseCard 1768–2083), scripts/parser-coverage.ts, scripts/parse-snapshot.ts (why no `CardDef` field may be added), scripts/scripts-queue.ts (selection language, `buildWave`), src/cards/taxonomy.ts, src/cards/scriptState.ts (`poolRows`), docs/workflows/vocab-wave.js and `C:/Users/vprog/AppData/Local/Temp/claude/C--Users-vprog-dev-mark-magic/92a3262f-7565-4bf0-a827-4f3a673c34b5/scratchpad/phase-9-1x.js` (the two templates), docs/workflows/README.md.

## Items

- **W-1 trace hook** (D1, "Hook in src/cards/parse.ts"): the module-level `ParseTraceRecord` array, `beginParseTrace` / `drainParseTrace` / `endParseTrace`, the hook sites listed in D1 (line loop, paragraph sentence, the `sentenceEffects` decompositions restructured as `parts.map(...)` + `.flat()` so partial results are visible, the trace-only condition probe, give-up, parseCost, "activate only if", the triggered branch, keyword bail-out, `unknown(def, line)`, back-face save/restore). Every site is one `if (parseTrace)` null test. **No new field on CardDef.** Output must be byte-identical with tracing off AND on (the probe only runs under the trace flag and touches nothing observable).
- **W-2 test/parse-trace.test.ts** (D1 "Test"): the five synthetic pins, `drainParseTrace()` empty when off, and the identity check over the synthetic rows plus every 50th `poolRows()` row (skip without master.db).
- **W-3 scripts/parse-why.ts + `"parse:why"` npm script** (D1 "CLI"): flags, the `selectionIndex` / `selected` extraction from `buildWave` (both call sites use them; `scripts:queue` output must stay byte-identical — prove with a rebuild of one wave dir into a scratch `--out` and a diff against data/scripts/batches/10.1/001.json's `cards` array), per-(card,line) cause selection, `norm1` and `shape` keys, the JSON shape (`constructs`, `shapes`, `prefixes`, `nestedCauses`, totals), `--md`, top-25 on stdout. Runs in about `coverage:pool` time.
- **W-4 scripts/parse-wave-briefs.ts** (D2): deterministic grouping, ≤16 `items` + `residual`, `generic-<slug>` names checked against `ruleFamilyFiles()`, `suggestedHome` rules, byte-identical on a second run.
- **W-5 docs/workflows/parse-wave.js** (D3): vocab-wave.js skeleton; args `{ wave, base, argsFile, items }`; IMPL schema = vocab's + `pins`, `paperCoverage`, `parseDiffChanged`, `overClaimed`, `constructCards`; the HARD do-not-edit list (parse.ts, composition.ts, types/schema/scripts/db/pool.ts, src/engine/**, the four pinned test files, data/master/parse-snapshot.json); new files only (`src/cards/rules/<name>.ts`, `test/parser-<name>.test.ts`, `test/scenarios/<name>.ts` + the one-line scenarios.test.ts registration, `npm run gen:registry`); the proofs list; ONE correctness reviewer ("find a printed card this rule misparses": 20-card deterministic sample from parse:diff groups, parser-alone AST via the `.no-scripts` store as scripts/parse-snapshot.ts:38-44 does, scenario must fail with the rule file reverted), one fix round, one re-review; `mergeable = no blocking && !coreTouched && reviews >= 1 && overClaimed empty`; result shape consumable by the merge pipeline (branch, worktree, commit, mergeable, coreChangeNeeded, coverage, parseDiff, issues, plus the new fields). No backticks inside template literals.
- **W-6 docs/workflows/parse-core-slice.js** (D3): the scratchpad phase-9-1x.js with phases Implement (fable high) / Review (2 Opus lenses) / Fix 1 (one round + one re-review); args `{ items: '<path>', base }`; bumps `PARSER_VERSION` once (5→6) and the pin in test/parser-composition.test.ts:655; parse:diff explained not accepted; `MTG_SCRIPTS=0 fuzz 100`.
- **W-7 docs** — docs/workflows/README.md table rows for parse-wave.js and parse-core-slice.js; a short `parse:why` section in docs/vocabulary/README.md (what the histogram means, how a construct becomes a rule family); HANDOFF phase-log note.

## Proofs (report the exact output line)
- `npm run typecheck:all`; `node --test --import tsx test/parse-trace.test.ts`; `npm run parse:diff` → 0 changed (tracing is off in every existing caller); `npm run coverage:pool` numbers unchanged, then `git checkout -- data/master/parser-coverage.json`; `npm test` green (staples, render calibration, parser-registry untouched).
- `npm run parse:why -- --tier paper --select "edhrec<=5000 commander:legal pool:paper" --top 60 --out data/master/parse-why.json --md data/master/parse-why.md` — report the totals line and the top 10 constructs (weight, cards, finishes, stage, key); then the whole pool: `npm run parse:why -- --tier paper --top 60 --out <scratch>/parse-why-pool.json` and report the top 10 too.
- `npx tsx scripts/parse-wave-briefs.ts --in data/master/parse-why.json --wave 9.1p --base <HEAD> --max 16 --out data/scripts/batches/parse-wave-1.json` twice → byte-identical; report the 16 item names with cards/finishes/suggestedHome.
- Syntax check for both workflow files: `node -e "const s=require('fs').readFileSync('<file>','utf8').replace(/^export const meta/m,'const meta');new Function('args','agent','parallel','pipeline','phase','log','budget','workflow','return (async()=>{'+s+'})()');console.log('syntax ok')"`.
- `git diff --stat` touches only src/cards/parse.ts, scripts/, test/, docs/, package.json, data/master/parse-why.*, data/scripts/batches/parse-wave-1.json; no CRLF.

## Reviewer lens
Run every proof yourself. Then attack: (1) **parse identity** — with tracing on, `JSON.stringify(parseCard(row))` for 2,000 pool rows must equal the tracing-off result (write the scratch loop yourself); any difference is a blocker; (2) **the innermost fragment** — take 15 unparsed lines from the histogram's top constructs and check by hand that the recorded fragment really is the smallest unclaimed sub-clause (a fragment that is the whole sentence when a sub-clause obviously failed is a major); (3) **finishes** — pick 10 cards the report says a construct "finishes" and confirm each has exactly one unparsed line with exactly one cause; (4) **determinism** — the brief builder twice, the CLI twice with the same args → identical bytes; (5) **queue identity** — `scripts:queue` rebuilt into a scratch dir matches the committed manifest/cards; (6) **workflow scripts** — the HARD lists forbid parse.ts / the snapshot / the pinned tests, the reviewer lens samples parse:diff, `mergeable` needs `overClaimed` empty, no inner backticks, `rerun`-safe prompts; (7) hygiene — no CRLF, no scratch files, no `CardDef` field, parse:diff 0, coverage:pool unchanged. A snapshot hash movement or a parse output change is a blocker.
