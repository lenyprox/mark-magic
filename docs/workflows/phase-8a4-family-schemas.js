export const meta = {
  name: 'phase-8a4-family-schemas',
  description: 'Phase 8a-4: compose family zod schemas into the script validator (generated _schemas barrel), so family ops are scriptable — one Fable 5.1 high implementer on main, one Opus reviewer, at most two fix rounds',
  phases: [{ title: 'Implement', model: 'fable' }, { title: 'Review' }, { title: 'Fix' }],
}
const ROOT = 'C:/Users/vprog/dev/mark-magic'
const PLAN = 'C:/Users/vprog/dev/mark-magic/docs/plans/every-card-scripted.md'
const IMPL = { type: 'object', properties: {
  summary: { type: 'string' },
  filesChanged: { type: 'array', items: { type: 'string' } },
  filesAdded: { type: 'array', items: { type: 'string' } },
  proofs: { type: 'array', items: { type: 'object', properties: { check: { type: 'string' }, result: { type: 'string' } }, required: ['check', 'result'] } },
  openIssues: { type: 'array', items: { type: 'string' } },
  notes: { type: 'string' },
}, required: ['summary', 'filesChanged', 'filesAdded', 'proofs', 'openIssues'] }
const REVIEW = { type: 'object', properties: {
  refuted: { type: 'boolean' },
  issues: { type: 'array', items: { type: 'object', properties: { severity: { type: 'string', enum: ['blocker', 'major', 'minor'] }, file: { type: 'string' }, claim: { type: 'string' }, evidence: { type: 'string' } }, required: ['severity', 'claim', 'evidence'] } },
  checksRun: { type: 'array', items: { type: 'string' } },
  leftoverFiles: { type: 'array', items: { type: 'string' } },
}, required: ['refuted', 'issues', 'checksRun', 'leftoverFiles'] }
const HARD = `HARD RULES: work in the main checkout ${ROOT} on branch main. Do NOT git commit, branch, stash or push. Never create symlinks or junctions; never run 'npm run web:index', 'parse:accept', 'golden:accept', 'fidelity:accept'; never kill node processes you did not start. Write LF. TWELVE vocabulary-family agents run at the same time in git worktrees under ${ROOT}/.claude/worktrees/ — never touch those directories; they add src/engine/ops/<family>.ts + <family>.schema.ts, src/cards/rules/<family>.ts, test/scenarios/<family>.ts, docs/vocabulary/<family>.md and will be merged AFTER you, so your composer must work with ZERO families today and with theirs tomorrow (test it with a scratch family you delete before finishing). A second Claude session edits apps/web/lib/gl/**, apps/web/app/dev/**, apps/web/components/card/**, apps/web/workers/**, test/scenemap.test.ts, 'Cloud Ex reference.jpg', 'feedback 1.txt' — not yours, never touch or report them. Do not edit src/engine/game.ts / state.ts / characteristics.ts / legal.ts / parse.ts / src/cards/rules/**. Scratch files go under C:/Users/vprog/AppData/Local/Temp/claude/.`
const IMPL_PROMPT = `You are implementing Phase 8a-4 of the plan at ${PLAN}: plan section 1.5's "Validation without the TS compiler API" — the GENERATED \`_schemas.ts\` that composes the core zod schemas with every family's \`<family>.schema.ts\`, so that a script using a family op passes CardScriptChecked (stage 1 of scripts:verify) and the lint / vocab:doc / renderer gap list know the family vocabulary. Read first: docs/HANDOFF.md section 3 items 12 and 26 (why this is needed: today a family op fails schema before the registry stage ever sees it), src/cards/schema.ts (the hand-written core schemas, EFFECT_VARIANTS / CONDITION_VARIANTS / … and CardScriptChecked, and test/schema-types.test.ts which pins them to the types by compile-time equality), scripts/gen-registry.mjs (the barrel generator: src/engine/ops/_registry.ts and src/cards/rules/_registry.ts — extend it, do not write a second generator), src/engine/ops/types.ts (FamilyModule) and src/engine/ops/_example.ts, src/cards/lint.ts (\`discriminators\` and the vocabularies it derives from the schema), scripts/vocab-doc.ts and scripts/scripts-schema.ts (consumers of the schema), src/cards/render.ts (RENDERERS from the registry; gap reporting), test/lint-ops.test.ts (the barrel-up-to-date lint).

THE FAMILY CONTRACT (the twelve 9.1 agents were told exactly this — implement to it, do not change it): each family file src/engine/ops/<family>.schema.ts exports \`export const schema: FamilySchema = { effects?: ZodObject[], conditions?: ZodObject[], triggers?: ZodObject[], statics?: ZodObject[], amounts?: string[], costParts?: Record<string, ZodTypeAny>, asEnters?: ZodObject[], targetKinds?: string[] }\` where every list entry is a strict z.object whose discriminator is a z.literal ('op' for effects, 'kind' for conditions / statics / asEnters, 'on' for triggers), and amounts / targetKinds are string literals. Add \`FamilySchema\` to src/engine/ops/types.ts (a type-only import of zod is fine there; runtime zod stays out of the engine bundle — put the runtime type in a new src/cards/familySchema.ts if importing zod types into ops/types.ts would drag zod into the web worker; check apps/web's bundling of src/engine before deciding and say what you found).

DELIVERABLES:
1. scripts/gen-registry.mjs also generates src/cards/_schemas.ts (LF, '// GENERATED' header, deterministic order, byte-identical when re-run) that imports every src/engine/ops/<family>.schema.ts (files ending in .schema.ts, not underscore-prefixed) and exports the merged lists: FAMILY_EFFECT_VARIANTS, FAMILY_CONDITION_VARIANTS, FAMILY_TRIGGER_VARIANTS, FAMILY_STATIC_VARIANTS, FAMILY_AMOUNTS, FAMILY_COST_PARTS, FAMILY_AS_ENTERS, FAMILY_TARGET_KINDS, and a FAMILY_SCHEMAS record by family name. Duplicate discriminator literals across families, or against the core, are a hard error naming both files. With no families the file exports empty lists (the state today).
2. src/cards/schema.ts composes: EffectSchema = z.discriminatedUnion('op', [...core, ...FAMILY_EFFECT_VARIANTS]) and likewise for conditions / triggers / statics / asEnters; AmountCount and TargetSpec.kind literal lists widen with FAMILY_AMOUNTS / FAMILY_TARGET_KINDS; AbilityCost's object gains the family cost parts (loose keys are still rejected). The lazy/recursive schemas (Effect inside for-each etc.) must still work. test/schema-types.test.ts keeps pinning the CORE schema to the core types (the Equals pin cannot see families; say so in a comment) and gains a runtime test that a scratch family registered at test time (a temp .schema.ts is NOT acceptable — build the union from an in-memory FamilySchema through an exported \`composeSchemas(families)\` helper) makes a script with its op pass CardScriptChecked and a typo fail.
3. src/cards/lint.ts derives its vocabularies (discriminators, keywords, amount counts, target kinds, condition kinds) from the COMPOSED schema, so a family op is not "unknown" to the lint; scripts/vocab-doc.ts lists family ops under their family with their zod field names; scripts/scripts-schema.ts (data/scripts/schema.json) emits the composed schema; src/cards/render.ts's gap report keeps counting a family op without a FamilyModule.render template as a gap (no change unless needed).
4. test/lint-ops.test.ts (or the existing barrel lint) asserts _schemas.ts is up to date; 'npm run gen:registry' regenerates all three barrels.
5. Prove with a scratch family under src/engine/ops/_probe_family.schema.ts? NO — underscore files are skipped by design; instead create a real scratch family pair (src/engine/ops/zz_probe.ts + zz_probe.schema.ts) with one effect op and one condition, run gen:registry, write a script using them under a temp dir, run 'npm run scripts:verify -- --dir <tmp> --ids <id>' (or verifyCards) and show stage 1 passes and the op reaches stage 3; then DELETE the scratch family, regenerate, and show 'git status' clean of it.
6. Docs: docs/vocabulary/README.md gains the "Family schema" section (the contract above, verbatim, plus how the composer folds it); docs/HANDOFF.md: strike item 26 and the schema half of item 12.

PROOFS (exact output lines): 'npm run typecheck:all'; 'npm run typecheck:schema'; 'npm run verify:quick'; 'npm test'; 'node scripts/gen-registry.mjs' twice → up to date, no diff; 'npm run scripts:check' 0 problems; 'npm run scripts:schema' regenerates data/scripts/schema.json (tracked) deterministically; 'npm run vocab:doc' twice → no diff; 'npm run coverage:pool' unchanged (12,200 / 12,065 with scripts on main 959901d) then 'git checkout -- data/master/parser-coverage.json'; 'npm run parse:diff' 0; the scratch-family proof of item 5; 'git status' shows only your files; no CRLF. ${HARD}

Return the structured result.`
const REVIEW_PROMPT = impl => `You are an adversarial reviewer of Phase 8a-4 (family schema composition) in ${ROOT} (main checkout, UNCOMMITTED working tree — do NOT commit, modify, stash or checkout; twelve family agents work in worktrees under .claude/worktrees — never touch them; a second session's web files are dirty — ignore them). Implementer report: ${JSON.stringify(impl).slice(0, 6000)}. Default to refuted=true unless you fail to find a real defect after running the checks yourself: 'npm run typecheck:all', 'npm run verify:quick', 'npm test', 'node scripts/gen-registry.mjs' twice, 'npm run scripts:check', 'npm run vocab:doc' twice, 'npm run coverage:pool' (then git checkout the report), 'npm run parse:diff'. Then attack: write your OWN scratch family (src/engine/ops/zz_review.ts + zz_review.schema.ts following the contract in docs/vocabulary/README.md) with an effect op, a condition, a trigger, a static, an amount count, a cost part and a target kind; regenerate; prove a script using each passes CardScriptChecked and that a duplicate discriminator against the core ('destroy') and against another scratch family is a hard error naming both files; prove lint accepts the family's amount count and target kind; prove the renderer reports the op as a gap when it has no template and not when it has one; delete both scratch files, regenerate, confirm 'git status' clean of them. Check the web worker bundle concern (does src/engine/ops/types.ts now import zod at runtime? 'npm run web:typecheck' and a grep of the import). Severity: blocker = a family op still cannot pass stage 1, or the barrel is nondeterministic, or zod leaks into the engine runtime; major = a consumer (lint, vocab:doc, schema.json) still ignores families; minor = everything else. Delete scratch files; list leftovers.`
phase('Implement')
const impl = await agent(IMPL_PROMPT, { label: 'impl:8a-4', phase: 'Implement', model: 'fable', effort: 'high', schema: IMPL })
if (!impl) return { failed: 'implementer produced nothing' }
log(`implementer: ${impl.summary.slice(0, 200)} open=${impl.openIssues.length}`)
phase('Review')
let review = await agent(REVIEW_PROMPT(impl), { label: 'review:8a-4', phase: 'Review', model: 'opus', effort: 'high', schema: REVIEW })
let blocking = review ? review.issues.filter(i => i.severity !== 'minor') : []
log(`review: ${blocking.length} blocker/major`)
const rounds = []; let current = impl
for (let round = 1; round <= 2 && blocking.length; round++) {
  phase('Fix')
  const fixed = await agent(`Fix the following blocker/major review issues from Phase 8a-4 in ${ROOT} (main checkout, uncommitted; do NOT commit). Issues: ${JSON.stringify(blocking)}. Fix each and pin it with a test, or refute it with evidence in notes; re-run every proof and report. ${HARD}`, { label: `fix:8a-4:${round}`, phase: 'Fix', model: 'fable', effort: 'high', schema: IMPL })
  current = fixed ?? current
  review = await agent(REVIEW_PROMPT(current) + ' This is a RE-REVIEW after a fix round: check each blocker/major was fixed and nothing regressed; do not re-open minors.', { label: `re-review:8a-4:${round}`, phase: 'Fix', model: 'opus', effort: 'high', schema: REVIEW })
  blocking = review ? review.issues.filter(i => i.severity !== 'minor') : []
  rounds.push({ round, fixed, review, remaining: blocking.length })
}
return { impl, rounds, final: current, lastReview: review, remainingBlocking: blocking, mergeable: blocking.length === 0 }
