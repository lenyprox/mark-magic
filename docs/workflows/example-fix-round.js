export const meta = {
  name: 'phase-8a3-fix-round-4',
  description: 'Phase 8a-3 fourth fix round: per-line substantive-effect budget, marker ops excluded, value-matched covers — Opus fixer plus adversarial re-review',
  phases: [{ title: 'Fix' }, { title: 'Re-review' }],
}
const MAIN = 'C:/Users/vprog/dev/mark-magic'
const WT = 'C:/Users/vprog/dev/mark-magic/.claude/worktrees/wf_b301c9c0-bff-1'
const BRANCH = 'worktree-wf_b301c9c0-bff-1'
const IMPL = { type: 'object', properties: { headCommit: { type: 'string' }, summary: { type: 'string' }, filesChanged: { type: 'array', items: { type: 'string' } }, proofs: { type: 'array', items: { type: 'object', properties: { check: { type: 'string' }, result: { type: 'string' } }, required: ['check', 'result'] } }, openIssues: { type: 'array', items: { type: 'string' } } }, required: ['headCommit', 'summary', 'filesChanged', 'proofs', 'openIssues'] }
const REVIEW = { type: 'object', properties: { refuted: { type: 'boolean' }, issues: { type: 'array', items: { type: 'object', properties: { severity: { type: 'string', enum: ['blocker', 'major', 'minor'] }, file: { type: 'string' }, claim: { type: 'string' }, evidence: { type: 'string' } }, required: ['severity', 'claim', 'evidence'] } }, checksRun: { type: 'array', items: { type: 'string' } }, leftoverFiles: { type: 'array', items: { type: 'string' } } }, required: ['refuted', 'issues', 'checksRun', 'leftoverFiles'] }
const RULES = `Work in the existing worktree ${WT} on branch ${BRANCH} (cd there; node_modules installed; HEAD is 5ebd6bf). Never touch ${MAIN} directly; never create junctions; never run 'npm run web:index'; never kill node processes you did not start; write LF. Do not edit src/engine/**, src/cards/parse.ts, src/cards/types.ts (beyond the existing one-line change) or src/cards/db.ts (beyond the existing one-line change). First confirm 'git status' is clean.`

const FIX = `Fourth fix round for Phase 8a-3. The re-review after round 3 found: BLOCKER 1 — abilityClaimLines lets one ability claim every line in its multi-line text with no per-line behaviour test; BLOCKER 2 — abilityIsSubstantive counts parser-internal fold-marker ops (fold-new-targets, alt-if-target, alt-take, fold-counter-if-yours, alt-kicked-amount, fold-restriction, fold-alt-mana) as behaviour; MAJOR — covers rule (iii) is shape-only (the declared value is never matched to the line; 'cycling' unanchored; entersTapped regex loose). ${RULES}

IMPLEMENT (decisions):
R1. Substantive effects. Define MARKER_OPS = the parser-internal marker ops listed in src/cards/types.ts under 'parser-internal markers folded into the previous effect' (alt-if-target, alt-take, fold-counter-if-yours, alt-kicked-amount, fold-restriction, fold-alt-mana, fold-new-targets) plus 'unknown'. substantiveCount(effects) counts effects whose op is not in MARKER_OPS, where container ops (conditional, optional-then, optional-pay, choose-mode, delayed-trigger, gain-ability, and any op with nested Effect[] fields — walk generically) count as 1 only if they contain ≥ 1 substantive effect recursively, and 'choose-mode' counts as the number of substantive modes. A static ability is substantive iff its effect kind is not 'unknown'. An activated mana ability with only add-mana is substantive. Export the helper; scripts:check reports 'ability has no substantive effect' naming the ability.
R2. Per-line budget. An ability's text may name one or more oracle lines (split on newline after normalisation, exactly as the parser sees them). It claims those lines ONLY IF substantiveCount(effects) ≥ number of lines it names; otherwise it claims nothing and scripts:check reports 'ability names N lines but declares only M substantive effects'. A static/triggered/activated ability may name at most the one line that contains it (a triggered ability names one line; if its text spans two lines because Scryfall split a sentence, treat the joined text as one line only when the second part starts with a lowercase letter — document). Two abilities may not claim the same line (report 'line claimed twice').
R3. covers value matching. For every CoverKind compare the declared value with the line, anchored, case-insensitive on the normalised line: keywords — every comma-separated part is a declared keyword with the same parameter (ward {N} ↔ wardCost N; toxic N; protection from X ↔ protectionFrom includes X; landwalk type; bushido/rampage/firebending N); altCosts — the line's leading keyword and cost must correspond to one declared altCost: flashback/escape/evoke/warp/impending/buyback/dash/jump-start with cost.mana.raw equal to the line's mana symbols (normalise whitespace), escape also its exile count, pitch clauses ('you may pay N life and exile …' ↔ payLife N / exileFromHand filter present; 'you may exile a … card from your hand rather than pay' ↔ exileFromHand); asEnters — 'tapped' ↔ '~ enters tapped', 'tapped-unless' ↔ '~ enters tapped unless …', 'pay-life-or-tapped' with life N ↔ 'you may pay N life. If you don't, it enters tapped', 'counters' ↔ the counter name in the line ('with a +1/+1 counter', 'with N charge counters' — amount N when numeric), 'choose' ↔ 'as ~ enters, choose a creature type/color', 'discard-or-graveyard' ↔ Mox Diamond wording; costModifiers — delve/convoke/improvise ↔ the line is exactly that keyword; reduce ↔ line matches /^(affinity for|~ costs \\{.+\\} less to cast)/; kicker ↔ /^(multi)?kicker (\\{.+\\})$/ with the cost equal to kicker.raw; cycling ↔ /^([a-z]+cycling|cycling) (\\{.+\\})$/ anchored with cost equal to cycling.raw (typecycling requires cyclingSearch); entersTapped ↔ /^~ enters (the battlefield )?tapped\\.?$/ or with 'unless' iff entersTapped is the object form; morph ↔ /^(morph|megamorph|disguise) (\\{.+\\})$/ with cost raw and megamorph/disguise flags matching; cascade/storm/rebound ↔ the exact keyword line; dredge ↔ /^dredge (\\d+)$/ equal to dredge; graveyardReplacement ↔ 'exile it instead' → 'exile', 'shuffle' → 'shuffle'; protection ↔ /^protection from (.+)$/ every listed quality in protectionFrom; ward ↔ /^ward (\\{(\\d+)\\}|—.+)$/ with wardCost when numeric; additionalCosts ↔ /^as an additional cost to cast ~, (.+)$/ where the clause's verb (sacrifice|discard|pay .* life|exile|tap|return) matches a field on some declared additionalCosts entry; toxic/bushido/rampage/firebending ↔ '<keyword> N' with N equal. Unanchored regexes are not allowed anywhere in COVER_LINE_RE. Keep the table in ONE place used by both applyScript's claim computation and scripts:check/CardScriptChecked (so an invalid cover claims nothing at runtime too).
R4. Tests: for each of the 21 kinds one positive with a real card AND one negative with a mismatching value (wrong mana cost, wrong number, wrong counter name); a multi-line spell ability with fewer substantive effects than lines is rejected; a fold-marker-only ability is rejected; a spell whose one ability legitimately implements two lines with two substantive effects is accepted; duplicate claims rejected.
R5. README/_example/schema.json updated; scripts-check messages precise.
PROOFS (exact lines): npm run typecheck:all; npm test; npm run scripts:check → 0 problems; all previous adversarial scripts still REJECTED plus the two new ones (multi-line single-ability; marker-only ability; wrong-value covers for at least 5 kinds), and the legitimate script still ACCEPTED; coverage:pool 11541; verify:pool 11064/477; smoke --verify against ${MAIN}/data/bench/smoke.json; no CRLF. Commit as 'Phase 8a-3: per-line substantive claims and value-matched covers (review fixes 4)'; report the hash.`

phase('Fix')
const fixed = await agent(FIX, { label: 'fix4:8a-3', phase: 'Fix', model: 'opus', effort: 'high', schema: IMPL })
if (!fixed) return { failed: 'fixer produced nothing' }
log(`8a-3 fix round 4 committed ${fixed.headCommit}`)

phase('Re-review')
const re = await agent(`You are an adversarial re-reviewer of Phase 8a-3 after its fourth fix round in the worktree ${WT} (branch ${BRANCH}, commit ${fixed.headCommit}); cd there. Do NOT commit or modify files; never touch ${MAIN}; never create junctions; never kill node processes you did not start. Fixer report: ${JSON.stringify(fixed).slice(0, 6000)}. Default to refuted=true unless you fail to find a real defect after running the checks yourself. The question, as before: can ANY script that passes 'npm run scripts:check' with 0 problems make a card fullyParsed while some non-ignored oracle line has no substantive behaviour declared for it? Attack: multi-line abilities with padding effects (e.g. two 'draw 0' effects — is a zero-amount or self-cancelling effect 'substantive'? report it as a finding but grade it minor if the renderer/judge layer of the plan would catch it, major if it is a trivial mass bypass), marker ops nested inside containers, containers with only markers, 'choose-mode' with empty modes, gain-ability granting empty abilities, covers with values that differ only in whitespace/case/unicode ('—' vs '-'), a keyword line with a parameterised keyword declared without its parameter, split second faces, mode extend where the parser already claims a line via a fragment, and any regex you can find unanchored. Write each attack as a temporary script under a real oracle id shard, run scripts:check --ids, record the verdict, delete it, confirm 'git status' clean. Re-run typecheck:all, npm test, scripts:check, coverage:pool (11541), verify:pool (11064/477), smoke --verify. Report issues with severity, file, claim, evidence and leftover files.`, { label: 're-review4:8a-3', phase: 'Re-review', model: 'opus', effort: 'high', schema: REVIEW })
return { fixed, rereview: re, mergeable: !!re && !re.issues.some(i => i.severity !== 'minor') }