export const meta = {
  name: 'phase-9-0c-bindings-filters',
  description: 'Phase 9.0c: engine bindings, scopes, filter fields and parser defects reported by the 9.0b parser work — one Fable 5.1 high implementer in the main checkout, two adversarial Opus reviewers, at most two fix rounds',
  phases: [{ title: 'Implement', model: 'fable' }, { title: 'Review' }, { title: 'Fix 1' }, { title: 'Fix 2' }],
}
const ROOT = 'C:/Users/vprog/dev/mark-magic'
const PLAN = 'C:/Users/vprog/dev/mark-magic/docs/plans/every-card-scripted.md'
const IMPL = { type: 'object', properties: {
  summary: { type: 'string' },
  filesChanged: { type: 'array', items: { type: 'string' } },
  filesAdded: { type: 'array', items: { type: 'string' } },
  delivered: { type: 'array', items: { type: 'string' } },
  coverage: { type: 'object', properties: { before: { type: 'string' }, after: { type: 'string' }, ownerDecksBefore: { type: 'integer' }, ownerDecksAfter: { type: 'integer' } }, required: ['before', 'after', 'ownerDecksBefore', 'ownerDecksAfter'] },
  parseDiff: { type: 'string' },
  goldenDiff: { type: 'string' },
  proofs: { type: 'array', items: { type: 'object', properties: { check: { type: 'string' }, result: { type: 'string' } }, required: ['check', 'result'] } },
  openIssues: { type: 'array', items: { type: 'string' } },
  notes: { type: 'string' },
}, required: ['summary', 'filesChanged', 'filesAdded', 'delivered', 'coverage', 'parseDiff', 'goldenDiff', 'proofs', 'openIssues'] }
const REVIEW = { type: 'object', properties: {
  refuted: { type: 'boolean' },
  issues: { type: 'array', items: { type: 'object', properties: { severity: { type: 'string', enum: ['blocker', 'major', 'minor'] }, file: { type: 'string' }, claim: { type: 'string' }, evidence: { type: 'string' }, cr: { type: 'string' } }, required: ['severity', 'claim', 'evidence'] } },
  checksRun: { type: 'array', items: { type: 'string' } },
  leftoverFiles: { type: 'array', items: { type: 'string' } },
}, required: ['refuted', 'issues', 'checksRun', 'leftoverFiles'] }
const HARD = `HARD RULES: work in the main checkout ${ROOT} on branch main. Do NOT git commit, do not create branches, do not stash, do not push. Never create symlinks or junctions. Never run 'npm run web:index'. Never kill node processes you did not start. Write every file with LF (no \\r). Never run 'npm run parse:accept', 'golden:accept' or 'fidelity:accept' — the orchestrator accepts after reading your diff summaries; restore regenerated tracked reports with 'git checkout -- data/master/parser-coverage.json'. TWO OTHER SLICES run at the same time in git worktrees under ${ROOT}/.claude/worktrees/ (8c: scripts:verify + renderer + src/cards/lint.ts + src/cards/render.ts + src/verify/probes.ts + scripts/scripts-verify.ts; 8k: src/cards/scriptState.ts, src/cards/taxonomy.ts, scripts/scripts-queue.ts and friends): never touch their worktrees, and do NOT edit src/cards/scripts.ts, src/verify/sandbox.ts or package.json (8c owns them) — if the structural gate needs a new magnitude rule for something you add, or a new npm script, write it in openIssues for the orchestrator. A SECOND Claude session also works in this checkout on the web card renderer: its modified/untracked files (apps/web/lib/gl/**, apps/web/app/dev/**, apps/web/components/card/**, apps/web/workers/**, test/scenemap.test.ts, 'Cloud Ex reference.jpg', 'feedback 1.txt') are NOT yours — never touch, revert or report them; if test/scenemap.test.ts fails under 'npm test', say so separately and ignore it. Lines in game.ts/parse.ts/types.ts/characteristics.ts are very long: read with 'sed -n A,Bp file | cut -c1-600' or the Read tool and edit with exact-string edits. Python patch scripts: use raw strings ('\\b' is a backspace byte otherwise) and run "grep -cP '\\x08' src/cards/parse.ts" (0) after every parse.ts edit. Keep the surrounding code style. Scratch files go under C:/Users/vprog/AppData/Local/Temp/claude/, never inside the repo.`
const IMPL_PROMPT = `You are implementing Phase 9.0c of the approved plan at ${PLAN}: the engine bindings, player scopes, filter fields and built-in parser defects that the 9.0b parser work (now committed on main — read 'git log -5' and docs/HANDOFF.md sections 2, 3 items 19-24 and 6) had to decline or work around. Every item below is a wording family that stays unparsed today only because the ENGINE or the TYPES lack one thing; you deliver the engine side AND the parser side (this is a serial slice: you may edit src/engine/**, src/cards/types.ts, src/cards/schema.ts, src/cards/parse.ts, src/cards/rules/composition.ts and tests). Read first: docs/vocabulary/composition.md (binding rules, Ref resolution, §7-8 declines), src/engine/refs.ts, src/engine/game.ts noteAffected and its callers (the sacrifice-cost path ~line 648, 'destroy'/'exile'/'bounce' cases) and case 'counter' (~1044), objs(), src/engine/legal.ts ownTargetSpecs / targetOptionsFor / targetingEffects, src/engine/characteristics.ts matchesFilter + power/toughness/lastKnown, src/cards/types.ts Filter / TargetSpec / ScopeWho / Amount / StaticEffect / CoreTriggerEvent, src/cards/rules/composition.ts (whoWord, objectTarget's /spell|ability/ exclusion, the 'bound()' / withFrame discipline in test/parser-composition.test.ts), parse.ts parseTrigger's 'whenever … enters' templates (~the generic '(another|an?) (.+?) enters' before '(.+?) you control enters'), pm() (the '-X' sign), the TGT slot and parseFilterWords, parseStaticRaw's anthem fallthrough (~1263), src/cards/subtype-vocab.ts + scripts/gen-subtypes; test/scenarios/composition.ts and composition-cards.ts; scripts/parse-snapshot.ts; docs/plans/owner-decks-needs.md.

DELIVERABLES (engine + types + parser + tests + docs for each; cite CR in every scenario):
1. BINDINGS (HANDOFF item 23): noteAffected() in the ops that act on objects but never record them — pump, grant-keyword, counters, untap (including the 'all-you-control' / 'lands-you-control' sets: the untapped objects become 'those'), gain-control, the 'sacrifice' OP (the sacrificed objects, like the cost path already does), look-top (the looked-at cards), and 'counter' (the countered spell's card with LAST KNOWN controller and mana value taken while on the stack, so 'Its controller mills three' and 'where X is that spell's mana value' resolve — extend the binding frame with a per-object lki record if refs.ts needs it); objs() / bind-from-targets must resolve kind:'stack' refs (a countered spell) so 'bind from targets' before 'counter' works. Then drop the parser's repairs: remove objectTarget's /spell|ability/ exclusion and re-enable the 14 counterspell wordings (Dream Fracture, Access Denied, Undermine, Countersquall, Psychic Strike, Thought Collapse, Countermand, Dismal Failure, Punish Ignorance, An Offer You Can't Refuse, Glen Elendra Guardian, Vex, Didn't Say Please, Strix Serenade), Disciple of Bolas / Heart-Piercer Manticore (sacrifice binding), Puresight Merrow (look-top), Gideon Martial Paragon (untap set), Slave of Bolas (gain-control) — each with a behavioural scenario.
2. SCOPES: ScopeWho gains 'target-opponent' (ownTargetSpecs emits { kind: 'opponent' } for scoped / unless-pays / for-each / move / the prop amount; legal.ts offers only opponents for it; refs.ts resolves it) and 'owner-of-that' (the exiled card's owner creates …). unless-pays gains 'otherwiseAs': 'controller' (Rhystic Study, Mystic Remora: 'you may draw a card unless that player pays {4}' → the payer is the target/that player, the alternative acts as the item's controller). Re-enable 'target opponent <does>' in composition.ts whoWord (Sphinx of Enlightenment, Chimney Imp, Archive Trap, Questing Phelddagrif, Soldevi Heretic, Grasping Tentacles, Kaya Intangible Slayer …).
3. FILTERS (types.ts Filter + characteristics.ts matchesFilter + parseFilterWords + schema): notSubtypes ('non-Angel creature', 'non-Aura enchantment', 'non-Human creature card', 'non-Vampire creature' — 30 cards, and the Restoration Angel wording that hung 9.0b's sandbox), supertypes: legendary / snow (48 + 15), notKeywords ('without flying', 22), printed P/T (power/toughness exact, 'with power 4 or greater' if not already there), 'dealt damage by ~ this turn' (11; needs the damage log the engine keeps or a per-turn record on o.ext), adjective flags kicked / transformed / historic / multicolored / monocolored / enchanted / equipped / modified / attacking / blocking / tapped / untapped (90 cards; add only those matchesFilter can answer today, list the rest in openIssues), and 'allTypes' semantics: Filter.types is any-of today, so 'artifact creature' and 'Land creatures you control have flying' read as OR — add an explicit all-of form (e.g. typesAll?: true or a separate field) used by the parser for adjacent type words, keeping every existing parse byte-identical except the cards the anthem fallthrough (~1263, 'Land creatures … get +N/+N') currently gets wrong (list them). 'dies' trigger gains controller: 'opponent' ('Whenever a creature an opponent controls dies', 11).
4. TARGETS AND AMOUNTS: return-from-graveyard gains count / optional and its graveyard-card TargetSpec gains who ('Return up to two target creature cards from your graveyard to your hand', 38 cards incl. Life from the Loam — re-pin it in test/staples.test.ts); 'up to X target …' → an Amount count on TargetSpec where X is the spell's X; aggregate amounts over an ObjectSet: { agg: 'max'|'sum'|'min', prop: 'power'|'toughness'|'mv', over: ObjectSet } ('the greatest power among creatures you control', 'the total power of …'); 'that many' (the last evaluated amount: a 'last-amount' Amount bound in the frame); Amount on scry / surveil / mill where they take a fixed number today ('you scry X'). Static set-pt ('Enchanted creature has base power and toughness 9/9', Super State) as a StaticEffect variant applied in computeStaticMods at layer 7b.
5. LKI: power-of-source / toughness-of-source (and prop 'power' of a Ref that has left the battlefield) read last-known information after the object left (Mortis Dogs after its own pump deals 4, not 2) — CR 608.2h / 800.4-style LKI: use o.lastKnown where moveTo already records it.
6. BUILT-IN PARSER DEFECTS (behind parse:diff, PARSER_VERSION 4): parseTrigger template order so 'Whenever a creature you control enters' matches the 'you control' template instead of minting subtypes You/Control (740 pool cards, 358 previously "fully parsed" with a dead trigger — Jaws of Defeat, Scourge of Valkas); pm() reading '-X' as '+X' ('gets -X/-X until end of turn', Death Wind); the TGT slot swallowing 'target A and target B' into subtypes And/Target (route to a multi TargetSpec); 'All creatures have "…"' capturing subtype All; any remaining parseFilterWords path that mints a subtype outside src/cards/subtype-vocab.ts. Every one of these changes existing fully-parsed cards: report the count per defect and the gained/lost cards in parseDiff.
7. TESTS: extend test/scenarios/composition.ts / composition-cards.ts with ≥ 1 scenario per delivered item (bindings per op, each scope word, each filter field, LKI, the aggregate amounts, the set-pt static, the trigger-template fix with Jaws of Defeat, the sign fix with Death Wind), test/parser-composition.test.ts for every wording, unit tests for matchesFilter's new fields, clone/serialize round-trips for any new ext state. Update test/fixtures/op-allowlist.json ONLY by shrinking; add op-coverage probes for new amount / static / target kinds so 'npm run coverage:ops' stays at 0 not allowlisted.
8. DOCS: docs/vocabulary/composition.md (§7-8: move each delivered decline into the documented vocabulary; document the new filter fields and scopes for script authors), docs/HANDOFF.md section 3 (strike / amend items 19, 22, 23, 24 as appropriate; add what remains open).

PROOFS (exact output lines): "grep -cP '\\x08' src/cards/parse.ts" 0; 'npm run typecheck:all'; 'npm run verify:quick'; 'npm test'; 'npm run coverage:pool' before/after (overall + paper headline; then 'git checkout -- data/master/parser-coverage.json'); owner-deck cards fully parsed before/after (docs/plans/owner-decks-needs.md's loader; the number on main after 9.0b is in HANDOFF section 2); 'npm run parse:diff' summary (NOT accepted) with per-defect counts; 'npm run verify:pool' (no new sandbox-throws; a hang is a blocker — run it under 'timeout 900' and bisect with --ids if it hangs); 'npm run golden:check' — the trigger-template fix may change goldens: paste the first difference per golden and explain it (do NOT accept); 'npm run fidelity:check' (report; do NOT accept); 'npm run bench:games' (another session loads the machine: run twice, report both; ≥ 41 / 4.0 acceptable under load, explain otherwise); 'npm run coverage:ops' 0 not allowlisted; 'node scripts/gen-registry.mjs' up to date; 'git status' only intended files; no CRLF. ${HARD}

Return the structured result: 'delivered' lists every item above you completed; openIssues lists every one you could not, with the exact clause and what it needs.`
const LENSES = [
  { key: 'correctness', text: `Lens: RULES AND PARSE CORRECTNESS (cite CR numbers). For every delivered item construct a board where it could misbehave and run it as a scratch scenario (a file under C:/Users/vprog/AppData/Local/Temp/claude/ importing test/scenarios/dsl.ts; 'node --test --import tsx <file>'): bindings after zone changes (a countered spell's LKI controller when the spell had been copied / when its controller lost the game in a 3-player game), untap-all binding only the objects that actually untapped, sacrifice-op binding vs the cost path, look-top binding hidden cards and redaction, target-opponent never offering the controller (2p and 4p), owner-of-that for tokens and stolen cards, unless-pays otherwiseAs, notSubtypes against changelings (CR 702.73: a changeling is every creature type, so 'non-Angel' excludes it), supertype filters after a type-changing effect, notKeywords against granted keywords, allTypes on artifact creatures, dies with controller 'opponent' for a stolen creature, return-from-graveyard count/optional with fewer cards than N, aggregate amounts on an empty set (0), 'that many' after a zero amount, LKI power after the source died in combat, the trigger-template fix ('Whenever a creature you control enters' must not fire for an opponent's creature; 'Whenever another creature enters' unchanged), the -X sign fix. Then run 'npm run parse:diff', read EVERY group, sample ≥ 40 changed cards across groups and compare oracle text to AST field by field; an over-claimed line or a wrong parse that changes outcomes is a blocker. Confirm 'npm run golden:check' differences (if any) are each explained by a listed card.` },
  { key: 'integration', text: `Lens: ENGINE INTEGRATION AND GATES. Verify yourself: "grep -cP '\\x08' src/cards/parse.ts" 0; PARSER_VERSION 4; 'npm run typecheck:all'; 'npm run verify:quick'; 'npm test'; 'npm run coverage:pool' numbers match the report (then 'git checkout -- data/master/parser-coverage.json'); 'npm run parse:diff' counts match; 'npm run verify:pool' under 'timeout 900' (no hang, no new throws); 'npm run golden:check' / 'npm run fidelity:check' outputs match the report; 'npm run bench:games' twice; 'npm run coverage:ops'; 'node scripts/gen-registry.mjs' up to date; 'git diff --stat -- src/cards/scripts.ts src/verify/sandbox.ts package.json' is EMPTY (8c owns them); no file under .claude/worktrees was touched; the second session's files were not touched. Read the diff for: per-call cost on the hot path (noteAffected allocations per pump/untap in AI rollouts — measure with bench and by counting allocations in a 100-game batch if bench is noisy), new ext / lki state JSON-plain and deep-copied by clone.ts / serialized / redacted (write a scratch round-trip test), Math.random / Date, N-player assumptions in target-opponent and owner-of-that, schema/type equality (typecheck:schema), 'npm run scripts:check' still 0 problems, the built-ins-first discipline in every parse.ts edit, docs matching code. Delete scratch files; list leftovers.` },
]
const reviewPrompt = (lens, impl, again) => `You are an adversarial reviewer of Phase 9.0c (engine bindings, scopes, filter fields, parser defects) in ${ROOT} (main checkout, branch main, UNCOMMITTED working tree — do NOT commit, do NOT modify the implementer's files, do NOT run parse:accept / golden:accept / fidelity:accept, do NOT stash, checkout or reset anything; two other slices run in worktrees under .claude/worktrees — never touch them; a second session's web-renderer files are also modified in this tree — ignore them). The plan is ${PLAN}. The implementer reported: ${JSON.stringify(impl).slice(0, 7000)}. ${again ? 'This is a RE-REVIEW after a fix round: check that each blocker/major issue was actually fixed and nothing regressed; do not re-open minors.' : ''} Default to refuted=true unless you fail to find a real defect after actually running the checks. ${lens.text} Severity: blocker = wrong rules outcome, over-claimed line, broken gate, hang; major = a delivered item that misleads script authors or a real deck; minor = everything else (backlog). Report each issue with severity, file, claim, concrete evidence and CR. Never kill node processes you did not start; never create junctions; never run 'npm run web:index'.`
phase('Implement')
const impl = await agent(IMPL_PROMPT, { label: 'impl:9.0c', phase: 'Implement', model: 'fable', effort: 'high', schema: IMPL })
if (!impl) return { failed: 'implementer produced nothing' }
log(`implementer: delivered ${impl.delivered.length}, coverage ${impl.coverage.before} → ${impl.coverage.after}; owner decks ${impl.coverage.ownerDecksBefore} → ${impl.coverage.ownerDecksAfter}; open=${impl.openIssues.length}`)
phase('Review')
const reviews = (await parallel(LENSES.map(l => () => agent(reviewPrompt(l, impl, false), { label: `review:${l.key}`, phase: 'Review', model: 'opus', effort: 'high', schema: REVIEW })))).filter(Boolean)
let blocking = reviews.flatMap(r => r.issues.filter(i => i.severity !== 'minor'))
log(`reviews: ${reviews.length}/2 returned, ${blocking.length} blocker/major issues`)
const rounds = []
let current = impl
for (let round = 1; round <= 2 && blocking.length; round++) {
  const ph = `Fix ${round}`
  phase(ph)
  const fixed = await agent(`Fix the following blocker/major review issues from Phase 9.0c in ${ROOT} (main checkout, uncommitted working tree; do NOT commit; never run parse:accept/golden:accept/fidelity:accept). Issues: ${JSON.stringify(blocking)}. For each issue either fix it (pin it with a scenario or parser test) or refute it with evidence in notes. Then re-run the full PROOF list and report it with fresh coverage / parse:diff / golden / fidelity summaries. ${HARD}`, { label: `fix:9.0c:${round}`, phase: ph, model: 'fable', effort: 'high', schema: IMPL })
  current = fixed ?? current
  const re = await agent(reviewPrompt(LENSES[0], current, true), { label: `re-review:9.0c:${round}`, phase: ph, model: 'opus', effort: 'high', schema: REVIEW })
  blocking = re ? re.issues.filter(i => i.severity !== 'minor') : []
  rounds.push({ round, fixed, rereview: re, remaining: blocking.length })
  log(`fix round ${round}: ${blocking.length} blocker/major remaining`)
}
return { impl, reviews, rounds, final: current, remainingBlocking: blocking, mergeable: blocking.length === 0 }
