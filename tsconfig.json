{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "outDir": "dist",
    "rootDir": "src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "types": ["node"]
  },
  "include": ["src/**/*.ts"],
  "//": "_example.ts is the family template: dead at runtime, but its `declare module` blocks are global, so it is typechecked on its own (tsconfig.example.json, npm run typecheck:example) instead of adding phantom variants to every core union here.",
  "exclude": ["src/engine/ops/_example.ts"]
}
