# Cards that cannot be scripted until an engine family lands.
# One data/scripts/blocked/<2-hex>/<oracle_id>.json per card: { clause, needs[], wave, attempts }.
# Tracked (unlike reports/, batches/, drafts/ and _quarantine/) so the needs backlog is reviewable in git.
